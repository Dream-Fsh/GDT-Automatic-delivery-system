# 腾讯广告规范投放 SOP（批量新建）

## 1. 适用范围与边界

适用于腾讯广告「批量新建」场景，为一个或多个账户按统一规范搭建广告。

- 本 SOP 结束于「生成预览广告」并完成核对。
- 点击最终「提交」会在外部平台创建广告，必须由操作人当次明确确认后执行。
- 当账户、产品、品牌形象或落地页不满足统一配置条件时，停止使用「全部相同」或「统一配置」，改为按账户分别配置。

## 2. 开始前准备

先向需求方收集并核对以下信息；不要沿用上一项目的账户或资源。

| 项目 | 必填信息 |
| --- | --- |
| 账户 | 主体名称、账户 ID、账户数量 |
| 产品 | 产品名称或搜索关键词 |
| 品牌形象 | 用于搜索的主体关键词 |
| 定向 | 定向模板名称 |
| 素材 | 素材目录、时间范围、筛选指标和阈值 |
| 文案 | 要使用的完整文案原文 |
| 落地页 | 每个账户对应的落地页名称 |
| 出价预算 | 出价区间与一键起量预算 |

### 默认配置

| 配置项 | 规则 |
| --- | --- |
| 产品搜索关键词 | `1`；首次新建广告选择名称匹配 `1` 的产品 |
| 定向模板 | `6.3-腾讯-通投-fsh` |
| 品牌形象 | 使用主体关键词搜索，默认选择搜索结果第 1 条 |
| 素材目录 | 在目录选择器搜索 `快应用`，选择首项 `网三-快应用` |
| 素材筛选 | 近 7 天；花费 `>= 1`；目标转化量 `>= 1` |
| 文案 | `解锁福利,限时获取`、`号码提交,解锁福利` |
| 出价 | 随机出价 `110–130`；一键起量预算 `200` |

## 3. 搭建流程

### 步骤 1：导入并选择账户

1. 进入「批量新建」，在媒体账户区域点击「更换」。
2. 筛选方式选择「账户 ID」。
3. 录入账户 ID：
   - 单账户：直接在「请输入关键词」框粘贴单个 ID。
   - 多账户：点击搜索框后缀的「批量搜索」图标（`.cl-search-input__batch` 内的放大镜 svg），在弹窗中逐行录入 ID（每行一个，回车换行），录入完成后点击弹窗底部的「搜索」。
4. 在查询结果中核对账户 ID 集合与输入集合完全一致后，点击结果表格左上角表头的全选复选框一次，全选所有已验证行；不再逐个勾选账户。
5. 核对主体、账户数量和投放链路后，点击「确定」。

**核对点：** 已选账户数与需求方提供的数量一致；批量搜索结果行数应与输入 ID 数一致，不一致时核对是否有 ID 无效或重复。

### 步骤 2：新建广告与营销内容

1. 点击「新建广告」。
2. 打开抽屉后，**从「营销内容」板块开始，并严格按页面从上到下的顺序操作**；不得先跳到广告版位、转化或出价预算。
3. 在「营销内容」内依次完成：营销目的 → 推广产品 → 目标/产品选择 → 产品分配。
4. 若当前版本提供「更多目标」，在该控件中搜索 `表单预约` 并选择对应目标；不要绕过营销内容而单独寻找优化目标入口。
5. 推广产品选择「运营商产品」，承接方式选择「页面跳转」。
6. 无论单账户还是多账户，进入「选择产品」后默认选择「全部相同」。
7. 选择「全部相同」后会弹出业务单元提示；这是标准确认弹窗，默认点击「确定」继续。只有确认后平台实际报错，才停止并输出异常，不因该提示改为「分配到账户」。
8. 若出现来源账户提示，在提示内选定已导入账户作为来源账户，勾选确认后点击「确定」。
9. 在产品搜索框输入 `1` 后，点击右侧「刷新」；等待结果重新加载后，从真实末页逐页检查，选择产品名称与需求方提供的目标产品精确匹配的结果，并确认同步至全部账户。本流程中目标产品名为单字符 `1`，只能选择展示名称完全等于 `1` 的行；关键字命中、日期中包含数字 `1`、包名中包含数字 `1` 或列表第 1 条都不能作为自动选择依据。

**分页等待规则：** 输入产品关键字后必须先点击「刷新」。产品关键字搜索会异步重算结果页数；等待产品列表与当前页码稳定，再读取真实末页，并从该末页逐页向前检查。只有产品名称精确匹配需求方给出的目标产品时才勾选；无唯一精确匹配时停止并向需求方确认。刷新前显示的旧页码或旧产品行不能用于勾选。

**核对点：** 营销内容已按自上而下的顺序完成；产品已显示在已选区域，且应用范围为全部目标账户。初始的「加粉互动 + 微信公众号 + 手动版位」仅为页面默认状态，未配置前不得保存。

### 步骤 3：设置广告版位

1. 版位选择「智能版位」。
2. 探索策略选择「稳步探索」。
3. 仅保留以下三类版位：
   - 「微信公众号与小程序」：勾选微信公众号与小程序、微信新闻插件。
   - 「腾讯平台与内容媒体」：勾选 QQ 浏览器、QQ/腾讯音乐及游戏、腾讯新闻、腾讯视频。
   - 「腾讯营销联盟」。
4. 不选择微信视频号、微信朋友圈、腾讯营销电脑端。

**核对点：** 三个目标版位均处于选中状态；切换策略后如选项被清空，重新勾选再继续。

### 步骤 4：设置转化

1. 转化接入类型选择「API 上报」。
2. 转化目标点击「更多目标」，搜索并选择优化目标 `表单预约`。随后查看当前账户返回的转化列表；转化名称不固定，不得写死为「优投-表单预约」。默认情况下列表只有一个可选项时，选择该唯一项，并核对其优化目标为「表单预约」。
3. 转化分配规则选择「全部相同」。
4. 转化归因点击「快速选择」，选择「点击归因」。
5. 深度转化、深度目标和深度辅助保持关闭。

**核对点：** 已选目标明确显示为表单预约，归因为点击归因。

### 步骤 5：设置出价、预算与广告名称

1. 出价方式选择 `oCPM`，拿量策略选择「稳定拿量」，分配规则选择「全部相同」。
2. 选择「随机出价」，最小值填 `110`，最大值填 `130`。
3. 开启「一键起量」，预算填 `200`；日预算选择「不限」。
4. 投放时间选择「长期投放」，投放时段选择「不限」，广告状态保持「开启」。
5. 广告名称使用：

```text
智版-公众号+平台+联盟-<日期>_<时分秒><动态标号>
```

日期、时分秒和动态标号必须由名称输入框下方的通配符控件插入，不能手输尖括号文字；动态标号起始值为 `1`。

### 步骤 6：应用定向模板

1. 选择「手动定向」，分配规则选择「全部相同」。
2. 先检查当前模板列表：若列表已直接显示且名称完全匹配 `6.3-腾讯-通投-fsh`，可直接勾选；否则必须在定向模板搜索框输入并搜索 `6.3-腾讯-通投-fsh`，等待结果后只勾选精确匹配项。
3. 确认已选模板数为 `1` 后，点击「确定」应用至全部账户。

### 步骤 7：设置创意基本信息

1. 创意形式选择「组件化创意」，创意分配规则选择「全部相同」。
2. 品牌形象跳转选择「自定义」。页面默认的「视频号」不得保留；必须先完成此项，再继续跳转类型、落地页和行动按钮。
3. 按主体关键词搜索品牌形象，例如主体含「云启」则搜索 `云启`，含「瑞利」则搜索 `瑞利`。
4. 搜索完成后默认勾选第 1 条；确认右侧已选状态后点击弹窗内「确定」。
5. 主跳转类型选择「自定义落地页」，嵌套落地页类型同样选择「自定义落地页」。
6. 「按钮文案」下拉框选择「立即领取」。
7. 相似素材 AI 处理关闭，创意默认状态开启。

### 步骤 8：筛选并添加图片素材

0. 在「创意素材」为空时，点击该区域底部空状态中的「选择素材」文字入口进入「手动选取素材」；不要把禁用的卡片按钮或创意组内按钮当作入口。
1. 在「手动选取素材」中，多账户分配规则选择「平均分配」。
2. 创意组分配规则选择「平均分配」。
3. **创意组素材上限选择 `1`。** 页面中显示的素材数量或创意组数量只是结果，不能据此把上限改为其他数值。
4. 从创意组上方的全局「批量添加」下拉框选择「添加图片」；不要进入单个创意组内的「添加素材」。
5. 在素材目录选择器中搜索 `快应用`，选择首项 `网三-快应用`。原 `广点通-通信素材` 文件夹已删除，不得继续使用；若首项或精确目录不出现，停止并记录目录异常。
6. 打开「上传时间」日期范围筛选，在浮窗中点击「清空」，等待该浮窗关闭；再切换到素材「列表」视图。
7. 点击「自定义指标与排序」→ 选中「自定义指标」，在数据时间中填入具体的起止日期（默认近 7 天），关闭浮窗并等待列表按新的时间范围刷新。
8. 点击「花费」列表表头的筛选图标，在浮窗中依次添加并设置条件：
   - 选择指标「花费」，比较符「大于等于」，阈值 `1`
   - 添加条件，选择指标「目标转化量」，比较符「大于等于」，阈值 `1`
   - 点击「确定」，等待筛选结果刷新。
9. 分页改为 `100条/页`，等待列表（约 100 行）完全渲染并稳定后再继续；不要在切换分页后立即全选。
10. 勾选素材表格左上角复选框，全选筛选结果；核对已选数量与筛选结果一致后，点击素材库内的「提交」并等待素材写入完成。
11. 素材库弹窗关闭后，回到外层「手动选取素材」面板，点击右下角的「确定」，确认素材已写入创意组并等待该面板关闭。

**核对点：** 两项分配规则均为「平均分配」，创意组素材上限为 `1`，素材由全局批量添加入口写入，且外层素材面板已点击「确定」并关闭。

### 步骤 9：配置广告文案

1. 手动文案区的分配方式保持「全账户复用」。
2. 点击「还可以添加」新增第二个独立输入框，分别填写两条文案：
   - `解锁福利,限时获取`
   - `号码提交,解锁福利`
3. 开启「多文案测试」。
4. 在测试选择页将「创意文案上限」设为 `2`，仅勾选上述两条文案。
5. 确认右侧已选数量为 `2/2` 后点击「确定」。

**核对点：** 预览中每个创意均显示「文案：已选 2 条」。

### 步骤 10：配置落地页

1. 打开「选择落地页」，先比对需求方提供的账户—落地页映射。
2. 若所有账户的落地页名称完全一致，选择「统一配置」，在「请输入关键词」框输入落地页名称，点击**右侧搜索按钮**（不同前端版本文案可能是「搜索」或「查询」），勾选后确认右侧显示 `1/1`，再点击「确定」。
3. 若账户落地页不一致，选择「按账户分配」，逐账户搜索、勾选和核对。

**核对点：**
- 搜索到结果不代表已选择，必须勾选表格行并确认右侧已选数量变化。
- 搜索按钮的文案在不同前端版本里会变化（实测为「搜索」「查询」和图标式三种），但位置始终紧贴「请输入关键词」输入框右侧。
- 弹窗标题文字为「请选择落地页」（含子串「选择落地页」即可）；不要按精确标题匹配。

### 步骤 11：生成预览并核对

1. 确认素材已生成创意组、每个创意已选 2 条文案、落地页已覆盖所有账户。
2. 点击「生成预览广告」，等待生成完成。
3. 在预览中逐项核对账户、产品、版位、转化、出价、定向、品牌形象、按钮、素材、文案、落地页和广告名称。
4. 预览无误后，向操作人说明提交的平台、账户数和即将创建广告的影响，获得明确确认后才可点击最终「提交」。

## 4. 常见问题与处理

| 场景 | 正确处理 |
| --- | --- |
| 粘贴账户 ID 后没有结果 | 清除空格、全角字符和非换行分隔符；整理为每行一个纯数字 ID 后重新导入。 |
| 产品搜索为空 | 从最后一页开始向前逐页查看；不能只检查第一页。 |
| 「全部相同」弹出来源账户确认 | 必须选择来源账户并勾选确认后点击确定，不能直接关闭弹窗。 |
| 切换「稳步探索」后版位被清空 | 重新勾选三个指定版位后再保存。 |
| 搜不到表单预约 | 确认已进入「更多目标」，等待列表刷新后再搜索。 |
| 定向、品牌、落地页只搜索未生效 | 在结果行勾选后，确认右侧已选数量变化，再点击确定。 |
| 素材页没有「日均转化」 | 使用当前页面可选的「目标转化量」作为本 SOP 的筛选指标。 |
| 全选素材只选中一页 | 先改为 `100条/页`，再勾选左上角全选。 |
| 出现相似素材或风险提示 | 先核对提示素材是否属于本次筛选结果；确认保留后选择「忽略风险，继续」，否则移除并重新核对数量。 |
| 两条文案被识别为一条 | 不要在同一输入框换行；使用「还可以添加」创建两个独立输入框。 |
| 多文案测试默认只能选一条 | 将「创意文案上限」从 `1` 调为 `2`，再确认右侧为 `2/2`。 |
| 广告名称提示缺少时间或标号通配符 | 从「插入更多」插入日期、时分秒和动态标号；动态标号起始值为 `1`。 |
| 页面无即时反应 | 等待搜索结果、已选数量、成功提示或下一步按钮状态出现；不要按固定秒数盲目继续。 |
| 多文案测试已勾选 2/2，但自动化日志报「已选文案区域未出现目标文案」 | 这是误报：日志里读的是 `.card-header` 的 innerText，只有「已选：N/N 清空」标题；真正的已选条目在 `.select-wrapper-card` 卡片内。手动确认右侧卡片文案与目标一致即可；自动化侧需把验证容器改为 `.select-wrapper-card`。 |
| 选择落地页弹窗里按钮文字是「查询」（不是「搜索」），自动化找不到搜索按钮 | 这是广点通按钮文案在不同版本会变化（偶发为「搜索」、偶发为「查询」、还有图标式 `cl-search-input__submit`）。自动化应**始终取「紧贴请输入关键词输入框右侧」的可见按钮**，而不是按 innerText 严格匹配。 |

## 5. 提交前检查清单

- [ ] 目标账户已导入、勾选，主体和数量正确。
- [ ] 已先选择优化目标；产品已按规则同步至全部账户。
- [ ] 智能版位、稳步探索和指定三类版位配置正确。
- [ ] 转化目标为表单预约，归因为点击归因。
- [ ] 随机出价为 `110–130`，一键起量预算为 `200`。
- [ ] 长期投放、时段不限、广告状态开启；名称中的日期、时分秒、动态标号均为系统通配符。
- [ ] 定向模板为 `6.3-腾讯-通投-fsh` 且已实际应用。
- [ ] 品牌形象、按钮文案「立即领取」、落地页均已实际选中并确认。
- [ ] 素材目录、近 7 天、两项筛选条件、每页 100 条均已核对。
- [ ] 多账户分配和创意组分配均为「平均分配」，创意组素材上限为 `1`。
- [ ] 文案恰为两条指定文案，多文案测试已开启并选择 `2/2`。
- [ ] 预览内容无误，并已取得当次最终提交的明确确认。

## 6. 自动化实现要求

1. 将账户、产品、品牌关键词、落地页映射、文案、出价和预算做成输入参数，不写死在脚本中。
2. 每次点击、输入或选择后，读取可见的成功状态、已选数量或下一步状态；不要只依赖固定等待时间。
3. 对产品分页、来源账户确认、筛选结果、弹窗确认和资源写入设置显式校验点。
4. 账户、产品、品牌、落地页、权限或白名单不匹配时，停止并输出异常清单。
5. 自动化默认停在预览阶段；最终提交必须有人工明确确认，例如 `CONFIRM_SUBMIT`。

### 6.6 自动化实现关键约定（skill 开发参考）

下面这些约定来自「广点通投放控制台」扩展（`chrome-extension/background.js`）的实测验证，是开发浏览器自动化 skill 时必须沿用的，否则会出现误选、误报。

#### 6.6.1 文案弹窗与表格 DOM 选择器

| 区域 | 选择器 | 说明 |
| --- | --- | --- |
| 文案弹窗 | `.cldrawer-dialog.program-creative-material.common-dialog.ad-dialog.gdt-text-dialog` | 弹窗根节点；同时匹配文本标记「多文案测试」+「请输入文案关键词」 |
| 文案表格 | `.fix-table` | cell 顺序：`[复选框, 文案, 创建者, 拒审次数, 3日点击率, 30日点击率, 3日消耗, 30日消耗, 创建时间]` |
| 已选区域 | `.select-wrapper-card` | **必须用整个卡片**，不要用 `.card-header`（只有标题） |

#### 6.6.2 文案列提取规则

`tbody tr` 的 `innerText` 是整行的统计列拼起来的，不能直接拿来比文案。提取文案原文的算法：

1. 遍历行内每个 `td`；
2. 跳过 `innerText` 为空、长度 < 2、纯数字 / 百分比 / `¥` / 短横线、空格组成的 cell；
3. 跳过形如 `YYYY-MM-DD` 开头的日期 cell；
4. 取第一个命中条件的 cell 文本作为该行的「文案」。

#### 6.6.3 选行打分算法（替代 `.includes()` 模糊匹配）

表格里常出现「`号码提交,解锁福利`」与「`号码提交,解锁福利#移动权益！`」这种短 / 长两条同前缀的行，单纯 `.includes(keyword)` 会选到带后缀的，导致后续验证失败。打分公式：

| 条件 | 分 |
| --- | --- |
| `copyText === keyword` | 100 |
| `copyText.startsWith(keyword + "#")` | 80 |
| `copyText.startsWith(keyword + ",")` | 75 |
| `copyText.startsWith(keyword + " ")` | 70 |
| `copyText.startsWith(keyword)` | 60 |
| `copyText.includes(keyword)` | 40 |
| 其他 | -1（不入选） |

排序：分数降序 → 分数相同时按 `copyText.length` 升序（短的优先，即更接近精确匹配）。最终取 `slice(0, maxCount || 1)` 选中。命中后回传 `matched: string[]` 到日志，方便后续追溯。

#### 6.6.4 已选区域验证容器

验证「已选文案区域是否包含目标关键词」时，**容器必须是 `.select-wrapper-card`**，不能用 `.card-header` 或 `.vue-recycle-scroller`。前者包含「已选：N/N 清空 + 已选条目列表」完整文本，后者只是标题。容错写法：

```js
const container = [...document.querySelectorAll(".select-wrapper-card")]
  .find(node => visible(node) && clean(node.innerText).includes("已选"));
if (!container) return { ok: false, error: "未找到已选文案区域。" };
const text = clean(container.innerText);
const missing = keywords.filter(k => !text.includes(k));
return { ok: missing.length === 0, missing };
```

#### 6.6.5 SOP 强约束

- 文案永远选 2 条（`job.copyOne`, `job.copyTwo`），创意组素材上限 `1`，多文案测试必须开启且上限 `2`。
- 选行操作完成后必须做「已选区域验证」再点击确定；验证失败不要点确定，让操作人介入。
- 表单字段改动以「系统可见的已选数量 / 提示文本」为最终信号，不依赖固定 `setTimeout`。

#### 6.6.6 「选择落地页」弹窗的搜索按钮匹配规则

广点通这个弹窗在不同前端版本里，搜索按钮的呈现方式有 3 种：

| 形态 | 识别方式 | 备注 |
| --- | --- | --- |
| 图标按钮 | `button.cl-search-input__submit` + `title="搜索"` | `innerText` 为空，靠 title 识别 |
| 文本按钮「搜索」 | `innerText === "搜索"` | 旧版前端 |
| 文本按钮「查询」 | `innerText === "查询"` | 新版前端（截图实测确认） |

**容错写法**：在弹窗范围内先找到 `placeholder` 含「请输入关键词」的输入框，再取它右侧最近的可见按钮（`bRect.left >= inputRect.right - 20` 且垂直 ±80px 内），不强求 innerText 严格相等。

弹窗识别：必须用 `clean(dialog.innerText).includes("选择落地页")`（弹窗真实标题为「请选择落地页」，含子串「选择落地页」即可）。**不要用 `===` 严格相等，否则永远匹配不上**。

行勾选：表格行 = `dialog.querySelectorAll("tr, [role=row], .el-table__row")`；勾选用行内 `label.el-checkbox`。`ok` 判断要看 `input.checked` 或 `label.classList.contains("is-checked")`，二选一为真即可。

#### 6.6.7 「选择落地页」弹窗的 DOM 结构与「按账户分配」交互

弹窗实际 DOM 并非标准 `.el-dialog`，不能用 `[].filter(d => d.classList.contains("el-dialog"))` 定位。实测结构：

```
弹窗容器（自定义组件，无 .el-dialog 类）
├── 标题文字：「请选择落地页」/「选择落地页」
├── 分配规则单选：label.el-radio-button（文案「统一配置」/「按账户分配」）
├── 搜索区：input[placeholder="请输入关键词"] + button（搜索/查询/图标）
├── 账户列表（仅「按账户分配」模式可见）
│   └── .account-list-wrapper
│       └── .account-list-content
│           └── .account-wrapper
│               └── .account-content-wrapper（选中时加 .checked 类）
│                   └── span.account-name[title="账户ID"]
└── 搜索结果区：.table-body > .el-checkbox-group > label.el-checkbox
    └── 每一行就是一个 label.el-checkbox（不是 tr）
        ├── span.el-checkbox__input > span.el-checkbox__inner + input[type=checkbox]
        └── span.el-checkbox__label > .package > .package__col[title="落地页名称"]
```

**弹窗定位容错写法**（`findLandingPageDialog`）：
1. 先尝试标准 `.el-dialog, [role=dialog]` 且 innerText 含「选择落地页」；
2. 找不到时，按标题文字 `=== "请选择落地页"` 反查父级容器，直到包含 `.account-list-wrapper` 或 `input[placeholder="请输入关键词"]`。

**分配规则单选定位**：不能用 `selectRadio(label, "选择落地页")`（该函数依赖 `.el-dialog` 作用域）。应使用 `selectRadioInSection(tabId, "落地页分配规则", "统一配置" | "按账户分配")`，按区域标题定位。

**统一配置模式**：搜索 → 等待结果渲染稳定（`stabilize` 至少 1200ms）→ 勾选 → 点击「确定」。

**按账户分配模式**逐账户流程：
1. 在 `.account-list-wrapper` 内找到 `span.account-name[title="账户ID"]`，点击其 `.account-content-wrapper`（未选中时 `click()`，已选中跳过）；
2. 填写搜索关键词 → 点击搜索按钮 → 等待结果出现 → `stabilize` 等待渲染稳定；
3. 勾选：直接匹配 `label.el-checkbox`（一行即一个 label），通过 `label.innerText` 或子元素 `title` 属性匹配落地页名称，点击 `label` 完成勾选；
4. 等待 `input.checked` 或 `label.classList.contains("is-checked")` 生效（最多 800ms）。

**搜索结果行匹配关键点**：
- 结果区不是 `<table>`，而是 `.el-checkbox-group > label.el-checkbox`；
- 行内文字可能因 CSS 截断（`.ellipsis`）不完整，必须同时检查子元素 `[title]` 属性；
- 匹配函数应同时检查 `clean(label.innerText).includes(name)` 和 `label.querySelector('[title]').getAttribute('title').includes(name)`。

#### 6.6.8 「确定」按钮的文案空格问题

Element UI 的 `el-button` 在两字按钮上会渲染为「确 定」（中间一个空格）。`clean()` 函数会把多个空白折叠为单个空格但不会移除，导致 `text === "确定"` 和 `text.includes("确定")` 均失败。

**处理方式**：点击「确定」时 labels 数组必须同时包含 `"确 定"` 和 `"确定"` 两种写法：`click(tabId, ["确 定", "确定"])`。

#### 6.6.9 「选择媒体账户」弹窗的多账户批量搜索

多账户时关键词输入框为单行，无法一次录入多个 ID，需改用搜索框后缀的批量搜索弹窗。实测 DOM 与交互约定：

**批量搜索图标**：位于「选择媒体账户」弹窗（`.el-dialog` / `[role=dialog]`，innerText 含「选择媒体账户」）内，选择器 `.cl-search-input__batch .el-popover__reference`（带 `title="批量搜索"`，内含 `svg`）。点击它会 toggle 弹窗，**非幂等**，不能重试点击；点击后需轮询等待弹窗打开再继续。

**批量搜索弹窗**：`.el-popover.el-popper`，innerText 含「请输入账户ID」。结构：

```
.el-popover
├── .textarea-header：「请输入账户ID（1/1000）」+ 清空按钮
├── .search-type-tips：提示「每一行为一个关键词，批量搜索将执行精确匹配」
├── .textarea-editor > .textarea-wrapper（每行一个）
│   └── .content > .input-group
│       ├── .el-input > input.el-input__inner（placeholder="请粘贴或输入账户ID，回车可换行"，maxlength=140）
│       ├── .input-suffix.btn > i.mg-icon-close（删除该行）
│       └── .input-suffix.len：「0/140」
└── footer.flex.flex-end：清空（default）+ 搜索（primary）按钮
```

**逐行录入关键点**：
- 每行一个 `input.el-input__inner`，取「最后一个可见 input」填入 ID；
- 非最后一行填完后必须派发 Enter 事件（`keydown`+`keypress`+`keyup`，`key/code/keyCode/which` 都要带）触发新增行，并轮询等待 input 数量增加后再填下一行；
- 填值用 `el.value = id` + `dispatch("input")` + `dispatch("change")` 触发 Vue v-model 更新；
- 录入操作**非幂等**，必须用 `evaluate` 一次性执行，不能用 `waitForAction` 重试（重试会覆盖已填行或重复回车）。

**搜索按钮**：弹窗 `footer` 内 `innerText === "搜索"` 的 primary 按钮。

**结果全选**：搜索后弹窗关闭，结果回到「选择媒体账户」表格。全选复选框在表头 `th.el-table-column--selection` 内的 `label.el-checkbox`（含 `span.el-checkbox__inner`），点击它一次全选所有数据行。**点击前先判断表头是否已选中**（`input.checked` 或 `label.is-checked`），已选中不要再点（会反选）。数据行选中数用 `tbody tr.el-table__row` 内 `label.el-checkbox` 的 `input.checked` / `.is-checked` 统计；点击后 Vue 异步更新，需等一帧（约 300ms）再统计，避免误判未选中。

#### 6.6.10 批量搜索入口的 DOM 识别与验证顺序

自动化必须限制在包含「选择媒体账户」的弹窗中，先定位普通关键词输入框 `input[placeholder="请输入关键词"]`，再选取它相邻的 `.cl-search-input__batch .el-popover__reference[title="批量搜索"]`。若该属性在当前版本缺失，只能使用相邻、语义标签为「批量搜索」的控制项；不得按图标外观、页面顺序或屏幕坐标点击。

批量入口是 toggle，只点击一次；随后必须验证弹层同时包含「请输入账户ID」、`input[placeholder="请粘贴或输入账户ID，回车可换行"]` 与「搜索」按钮。验证失败时停止并记录 DOM 快照，不能向普通关键词框输入多行账户。

搜索完成后，按完整 ID 集合比较实际结果与请求结果。只有集合完全一致时，才可勾选未选中的表头复选框；否则输出缺失、重复或额外账户。勾选后再次验证行复选框数和「已选 N/50」均等于预期数量，才可点击「确定」。截图仅用于诊断选择器问题。
